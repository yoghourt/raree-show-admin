/** Spike adapter — production Gate lives in lib/discovery/granularity-gate. */
export type {
  FrameNode,
  GranularityAnalysis,
  GranularityGateResult,
  GranularityInput,
  GranularityInvariant,
  GranularityLabels,
  GranularitySeverity,
  GranularityViolation,
  SourceHeading,
  StoryFrameBundle,
  StoryNode,
} from "../../lib/discovery/granularity-gate/types";
