# Renderer Capability Adaptation Spike

Same Visual Intent + Local model + seed; only **visual expression** changes (direct / action-based / symbolic).

Findings: [`docs/findings/capability-adaptation-validation-spike.md`](../../docs/findings/capability-adaptation-validation-spike.md)

```bash
npx tsx scripts/capability-adaptation-spike/run.ts
```
