# Discovery Visual Expression Ownership Spike

Compare:

- **Option A:** Discovery `visualIntent` → external adapter → Local
- **Option B:** Discovery `rendererExpression` (same call) → Local

Findings: [`docs/findings/discovery-expression-ownership-spike.md`](../../docs/findings/discovery-expression-ownership-spike.md)

```bash
npx tsx scripts/discovery-expression-ownership-spike/run.ts
```
