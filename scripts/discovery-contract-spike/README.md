# Discovery Visual Expression Contract Spike

Temporary contract between Discovery and Renderer:

- `visualIntent` — meaning
- `rendererExpression` — Local-executable visible form

Renderer consumes Expression only.

Findings: [`docs/findings/discovery-visual-expression-contract-spike.md`](../../docs/findings/discovery-visual-expression-contract-spike.md)

```bash
npx tsx scripts/discovery-contract-spike/run.ts
```

Candidate schema: `candidate-schema.json` · Types: `contract.ts`
