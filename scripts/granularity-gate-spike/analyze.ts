/** Spike adapter — production Gate lives in lib/discovery/granularity-gate. */
export {
  analyzeGranularity,
  bundleByStory,
  captionCorpus,
  concatenatedCaptions,
  estimateProgressionUnits,
  sharedProperNames,
  uncoveredTurns,
} from "../../lib/discovery/granularity-gate/analyze";
