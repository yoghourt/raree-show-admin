# SPIKE-RCS-001 — Local Renderer Capability Comparison

Frozen EVG-001-R3 Local prompts × three LocalAI models. No Projection / identity / prompt retuning.

```bash
npx tsx scripts/renderer-capability-spike/run.ts
```

Outputs: `results/{sd35,z-image,flux2-klein}/` — does not overwrite EVG-001-R3.
